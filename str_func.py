def first_func():
    '''
    принимает на вход строку и возвращает ее со всеми заглавными буквами + 
    '''
    user_text = input()
    return user_text.upper()


def second_func():
    user_text = input()
    return user_text.title()

# Попытка вызвать конфликт:)
# Попытка вызвать конфликт №2 :)


def conflict():
    return 404
    
# Попытка вызвать конфликт №3
# Есть вероятность того, что я запутался.